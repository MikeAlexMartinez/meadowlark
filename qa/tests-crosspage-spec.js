describe('check referrer fields for tour trips', function() {

	it('rRequesting a group rate quote from the hood river tour page ' + 
			'should populate the referrer field', function() {
		
		
		var referrer = 'http//localhost:3000/tours/hood-river';

		// Open the hood-river tour page
		browser.get('/tours/hood-river');


	});

});